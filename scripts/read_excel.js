const XLSX = require('xlsx'); const wb = XLSX.readFile('kurumsallaşma envanterii.xlsx', { cellFormula: true }); const ws = wb.Sheets[wb.SheetNames[0]]; console.log('Formula at D114:', ws['D114'] ? ws['D114'].f : 'N/A'); console.log('Value at D114:', ws['D114'] ? ws['D114'].v : 'N/A'); const csv = XLSX.utils.sheet_to_csv(ws); const fs = require('fs'); fs.writeFileSync('survey_data.csv', csv);
